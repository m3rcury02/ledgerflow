{
  "annotations": {"list": []},
  "editable": false,
  "graphTooltip": 1,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [{"expr": "sum by (stage, activity, outcome) (rate(ledgerflow_payment_provider_attempts_total[5m]))", "legendFormat": "{{stage}} {{activity}} {{outcome}}"}],
      "title": "Provider attempts and outcomes",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "s"}},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [{"expr": "histogram_quantile(0.95, sum by (le, stage, activity) (rate(ledgerflow_payment_provider_duration_seconds_bucket[5m])))", "legendFormat": "{{stage}} {{activity}} p95"}],
      "title": "Provider latency",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 8},
      "id": 3,
      "targets": [{"expr": "ledgerflow_payment_provider_circuit_state", "legendFormat": "{{state}}"}],
      "title": "Circuit state",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 8},
      "id": 4,
      "targets": [{"expr": "ledgerflow_payment_provider_bulkhead_available", "legendFormat": "available"}, {"expr": "ledgerflow_payment_provider_bulkhead_maximum", "legendFormat": "maximum"}],
      "title": "Provider bulkhead",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 8},
      "id": 5,
      "targets": [{"expr": "sum by (stage, outcome) (rate(ledgerflow_payments_outcomes_total[5m]))", "legendFormat": "{{stage}} {{outcome}}"}, {"expr": "sum by (state) (rate(ledgerflow_payments_state_transitions_total[5m]))", "legendFormat": "state {{state}}"}],
      "title": "Persisted payment outcomes and states",
      "type": "timeseries"
    }
  ],
  "refresh": "15s",
  "schemaVersion": 41,
  "tags": ["ledgerflow", "provider", "payment"],
  "time": {"from": "now-1h", "to": "now"},
  "timezone": "utc",
  "title": "LedgerFlow — Provider and Payment",
  "uid": "ledgerflow-provider-payment",
  "version": 1
}
