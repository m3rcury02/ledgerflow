ledgerflow:
  fault-injection:
    enabled: ${LEDGERFLOW_FAULT_INJECTION_ENABLED:false}
    point: ${LEDGERFLOW_FAULT_INJECTION_POINT:PAYMENT_PROVIDER}
    mode: ${LEDGERFLOW_FAULT_INJECTION_MODE:FAIL}
    delay: ${LEDGERFLOW_FAULT_INJECTION_DELAY:100ms}
