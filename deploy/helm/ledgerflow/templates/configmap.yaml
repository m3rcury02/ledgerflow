apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ledgerflow.fullname" . }}
  labels:
    {{- include "ledgerflow.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
