LedgerFlow deployed to namespace {{ .Values.namespace }}.

Reach the API from this host (ClusterIP only, no NodePort):

  kubectl --namespace {{ .Values.namespace }} port-forward svc/ledgerflow-api 8080:{{ .Values.api.containerPort }}
  curl --fail http://localhost:8080/api/v1/orders/...

Check rollout status:

  kubectl --namespace {{ .Values.namespace }} get deployments,pods,hpa,pdb

scripts/kind-smoke-test runs a full real order-creation proof end to end.
